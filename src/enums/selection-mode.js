/**
 * SelectionMode enum defines the selection modes of DataTable.
 * @readonly
 * @enum {string}
 */
const SelectionMode = {
    SELECTION: 'selection',
    INVERSE: 'inverse',
    ALL: 'all'
};

export default SelectionMode;
