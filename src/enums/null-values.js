/**
 * NullValuesRepr enum defines the format of null, undefined, invalid
 * values representation in DataModel.
 *
 * @readonly
 * @enum {string}
 */
const NullValuesRepr = {
    NULL: null,
    NA: 'NA'
};

export default NullValuesRepr;
